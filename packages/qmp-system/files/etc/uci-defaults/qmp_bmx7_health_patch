#!/bin/sh

if ! cat /etc/crontabs/root | grep bmx7health; then
	echo "* * * * * /etc/qmp/bmx7health.sh >> /tmp/log/bmx7health.log" >> /etc/crontabs/root
	/etc/init.d/cron enable
	/etc/init.d/cron start
fi
